# Plug-ins

Every product lives in its own directory and owns its source, tests, documentation, identifiers,
and build definition. Add future products as siblings of `loop-surgeon`; do not mix their files into
the repository root.

