name: Build downloadable Windows VST3

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "CMakeLists.txt"
      - "CMakePresets.json"
      - "plugins/loop-surgeon/**"
      - ".github/workflows/build-windows-release.yml"
  push:
    branches: [main]
    paths:
      - "CMakeLists.txt"
      - "CMakePresets.json"
      - "plugins/loop-surgeon/**"
      - ".github/workflows/build-windows-release.yml"

permissions:
  contents: write

concurrency:
  group: loop-surgeon-windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows-x64:
    runs-on: windows-2022
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure x64 Release
        run: cmake --preset vs2022-release

      - name: Build VST3 and Standalone
        run: cmake --build --preset build-release --config Release --parallel

      - name: Run engine tests
        run: ctest --preset test-release --output-on-failure

      - name: Package VST3 and PC guide
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & "plugins/loop-surgeon/scripts/package-windows.ps1" -Version "0.5.2-alpha"
          Copy-Item -LiteralPath "build/packages/Loop-Surgeon-0.5.2-alpha-Windows-x64-VST3.zip" -Destination "Loop-Surgeon-Windows-x64-VST3.zip"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Loop-Surgeon-Windows-x64-VST3
          path: Loop-Surgeon-Windows-x64-VST3.zip
          if-no-files-found: error
          retention-days: 30

      - name: Upload Windows package to alpha release
        if: github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $tag = "v0.5.2-alpha"
          if (-not (gh release view $tag 2>$null)) {
            gh release create $tag --target $env:GITHUB_SHA --title "Loop Surgeon 0.5.2 Alpha" --notes "Replaces correlated long-grain looping with source-modelled spectral texture synthesis, removing copied pass-by trajectories and comb-like tonal artifacts. Adds explicit Direct Seam Loop bounds, deterministic variations, project recall, and WAV export." --prerelease
          }
          gh release upload $tag "Loop-Surgeon-Windows-x64-VST3.zip" --clobber
          git tag "v0.5.2-alpha-windows-ready" $env:GITHUB_SHA
          git push origin "refs/tags/v0.5.2-alpha-windows-ready" --force
